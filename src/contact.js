//Module for rendering the contactTab
import { domElement } from "./factoryDOM";

export function renderContact() {

    const contact = domElement('div', {class: 'container-fluid'})
    return contact;
}

